window.Progress={render(){const s=Phoenix.state,avg=pct(s.mocks.reduce((n,x)=>n+x.score,0),s.mocks.length*100);$('progressArea').innerHTML=`<div class="stats"><div class="stat"><strong>${s.mocks.length}</strong><span>Mocks</span></div><div class="stat"><strong>${avg}%</strong><span>Average</span></div><div class="stat"><strong>${s.pbq.got}/${s.pbq.got+s.pbq.miss}</strong><span>PBQs</span></div><div class="stat"><strong>${s.flash.got}/${s.flash.got+s.flash.miss}</strong><span>Flashcards</span></div></div><h3>Mock History</h3>${s.mocks.map(m=>`<div class="result"><b>${m.date}</b><p>${m.score}% · MCQ ${m.ok}/${m.mc} · PBQ ${m.pbavg}%</p></div>`).join('')||'<p>No mocks yet.</p>'}<h3>Weak Topics</h3><div class="chips">${Object.entries(s.weak).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<span class="chip">${k} · ${v}</span>`).join('')||'<span class="chip">No weak topics.</span>'}</div>`}}
